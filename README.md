# Hold Back Fighter (HBF)  
- only effects the "hold back" tactic world dominator.
- Has no effect on the "run down" tactic world dominator.
# C++ program for best cyber warfare
- Use the make progam, uses g++ compiler for Linux.
- Use clang++ for other systems. A simple change to makefile.
- The Makefile is in GNU make format, will fail on UNIX, use gmake or convert it.
- hpf -q will run without the dots and bangs output. Use it if placed in systemd or rc.local
  or the system journaler will eat performance logging the dots and bangs.
# Harmful Effects
- This program is a disabler instead of a killer.
- They disable you, disable them back.
